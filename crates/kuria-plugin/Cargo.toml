[package]
name = "kuria-plugin"
version = "0.1.0"
edition = "2024"
description = "Plugin SDK for the Kuria Mail Server"
license = "MIT"

[lib]
crate-type = ["lib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
libc = "0.2"
